@extends('layouts.app') @section('title','Certificates') @section('content')<div class="page-head"><div><h1>Milestones that matter.</h1><p>{{ auth()->user()->isAdmin()?'Manage and verify your academy’s issued credentials.':'Your hard work, recognized. Download and share your achievements.' }}</p></div><a class="btn secondary" href="/verify">Verify a credential ↗</a></div><div class="grid cols-2">@forelse($certificates as $certificate)<article class="card pad stack" style="border-top:3px solid #b5a16d"><div class="row between"><x-icon name="award" style="width:40px;color:#b29c65"/><span class="badge {{ $certificate->revoked_at?'red':'green' }}">{{ $certificate->revoked_at?'Revoked':'Verified achievement' }}</span></div><div><small>{{ $certificate->number }}</small><h2 style="margin-top:10px">{{ $certificate->course_title }}</h2><p class="muted">Awarded to {{ $certificate->student_name }}</p></div><small>Completed {{ $certificate->completed_at->format('d F Y') }} · Issued {{ $certificate->issued_at->format('d F Y') }}</small><div class="row wrap">@unless($certificate->revoked_at)<a class="btn small" href="/certificates/{{ $certificate->id }}/download">Download PDF ↓</a>@endunless<a class="link" href="{{ route('verify',$certificate->verification_token) }}">Verify ↗</a>@if(auth()->user()->isAdmin()&&!$certificate->revoked_at)<form method="POST" action="/certificates/{{ $certificate->id }}/revoke" data-confirm="Revoke this certificate? The public verification page will show it as invalid.">@csrf<button class="btn danger small">Revoke</button></form>@endif</div></article>@empty<div class="card empty"><strong>Your achievements belong here.</strong>Complete a course and its required assessments to earn your first certificate.</div>@endforelse</div><div style="margin-top:25px">{{ $certificates->links() }}</div>@endsection
