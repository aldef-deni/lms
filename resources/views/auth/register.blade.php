@extends('layouts.auth') @section('title','Join the academy') @section('content')<h1>Make room for growth.</h1><p class="muted">Create your account and start your learning journey.</p><form method="POST" action="/register">@csrf @foreach(['name'=>'Full name','email'=>'Email address','password'=>'Password','password_confirmation'=>'Confirm password'] as $field=>$label)<div class="field"><label for="{{ $field }}">{{ $label }}</label><input id="{{ $field }}" name="{{ $field }}" type="{{ str_contains($field,'password')?'password':($field==='email'?'email':'text') }}" value="{{ str_contains($field,'password')?'':old($field) }}" autocomplete="{{ str_contains($field,'password')?'new-password':$field }}" required>@if($field==='password')<small>At least 10 characters, including letters and numbers.</small>@endif</div>@endforeach<button class="btn">Create your account →</button></form><p class="muted" style="text-align:center;margin-top:25px">Already a member? <a class="link" href="/login">Sign in</a></p>@endsection