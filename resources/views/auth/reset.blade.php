@extends('layouts.auth') @section('title','Choose a new password') @section('content')<h1>Choose a new password.</h1><form method="POST" action="/reset-password">@csrf<input type="hidden" name="token" value="{{ $token }}"><div class="field"><label>Email address</label><input type="email" name="email" value="{{ old('email',request('email')) }}" required></div>@foreach(['password'=>'New password','password_confirmation'=>'Confirm password'] as $field=>$label)<div class="field"><label>{{ $label }}</label><input type="password" name="{{ $field }}" required autocomplete="new-password"></div>@endforeach<button class="btn">Reset password</button></form>@endsection