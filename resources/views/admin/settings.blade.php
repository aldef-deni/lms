@extends('layouts.app') @section('title','Settings') @section('content')<div class="page-head"><div><h1>Academy settings</h1><p>A consistent identity across every learning experience.</p></div></div><form class="card pad" method="POST" action="/settings">@csrf @method('PUT')<div class="form-grid">@foreach(['app_name'=>'ALDEF LMS','logo_path'=>'assets/logo/aldef-landscape02.png','contact_email'=>'hello@aldeftech.com','certificate_prefix'=>'ALDEF-LMS','contact_address'=>''] as $key=>$default)<div class="field"><label for="{{ $key }}">{{ ucfirst(str_replace('_',' ',$key)) }}</label><input id="{{ $key }}" name="{{ $key }}" value="{{ old($key,$settings[$key]??$default) }}" @disabled(auth()->user()->role!=='super_admin')></div>@endforeach</div>@if(auth()->user()->role==='super_admin')<div class="form-actions"><button class="btn">Save settings</button></div>@else<p class="muted" style="margin-top:25px">System settings are managed by your Super Admin.</p>@endif</form>@endsection
