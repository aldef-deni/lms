<article class="card course-card"><a href="{{ route('courses.show',$course->slug) }}"><div class="course-art tone-{{ $loop->index%3 }}">@if($course->thumbnail)<img src="{{ asset('storage/'.$course->thumbnail) }}" alt="{{ $course->title }}" loading="lazy">@else<span class="eyebrow">ALDEF ACADEMY / {{ str_pad($course->id,2,'0',STR_PAD_LEFT) }}</span><span class="art-label">{{ $course->category?->name ?? 'Professional learning' }}</span>@endif</div></a><div class="pad"><div class="row between"><span class="badge">{{ ucfirst($course->level) }}</span><small>{{ $course->category?->name }}</small></div><h3><a href="{{ route('courses.show',$course->slug) }}">{{ $course->title }}</a></h3><p class="description">{{ Str::limit($course->description,100) }}</p><div class="divider"></div><div class="row between"><span class="row"><span class="avatar" style="width:30px;height:30px;font-size:11px">{{ mb_substr($course->instructor->name,0,1) }}</span><small>{{ $course->instructor->name }}</small></span><a class="link" href="{{ route('courses.show',$course->slug) }}" aria-label="View {{ $course->title }}">Explore →</a></div></div></article>