@extends('layouts.public') @section('title','Certificate verification') @section('content')<div class="container"><div class="verify-card stack"><div style="text-align:center"><span class="eyebrow" style="color:#8479c3">TRUSTED ACHIEVEMENTS</span><h1 style="margin:16px 0">Verify a credential.</h1><p class="muted">Confirm the authenticity of an ALDEF LMS certificate.</p></div><form method="GET" action="/verify" class="card pad row wrap"><input class="search" style="flex:1;min-width:200px" name="code" value="{{ request('code') }}" placeholder="ALDEF-LMS-2026-000001" aria-label="Certificate number" required><button class="btn">Verify</button></form>@if($lookup)<div class="card pad stack">@if($certificate&&!$certificate->revoked_at)<div class="row"><span class="avatar" style="background:#e4f5eb;color:#38825c"><x-icon name="check" style="width:23px"/></span><div><h2>Valid certificate</h2><p class="muted">This achievement was issued by {{ $brandName }}.</p></div></div><div class="divider"></div>@foreach(['Participant'=>$certificate->student_name,'Course'=>$certificate->course_title,'Certificate number'=>$certificate->number,'Completion date'=>$certificate->completed_at->format('d F Y'),'Issue date'=>$certificate->issued_at->format('d F Y'),'Instructor'=>$certificate->instructor_name] as $label=>$value)<div class="row between wrap"><small>{{ $label }}</small><strong>{{ $value }}</strong></div>@endforeach @else<span class="badge red">{{ $certificate?'Certificate revoked':'Certificate not found' }}</span><h2>This credential could not be validated.</h2><p class="muted">Check the certificate number or contact the academy for assistance.</p>@endif</div>@endif<p class="muted" style="text-align:center;font-size:12px">Scan the QR code on any ALDEF certificate to access this page directly.</p></div></div>@endsection
