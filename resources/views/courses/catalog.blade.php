@extends('layouts.public') @section('title','Course catalog') @section('content')<section class="section"><div class="container stack"><div class="page-head"><div><span class="eyebrow" style="color:#7771cc">THE ACADEMY</span><h1 style="margin-top:10px">Find your next learning path.</h1><p>Build practical skills at a pace that works for you.</p></div></div><form class="card pad row wrap" method="GET"><input class="search" style="flex:2;min-width:180px" name="q" value="{{ request('q') }}" placeholder="Search courses…" aria-label="Search courses"><select class="search" style="flex:1;min-width:160px" name="category" aria-label="Category"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->name }}</option>@endforeach</select><select class="search" style="flex:1;min-width:150px" name="level" aria-label="Level"><option value="">All levels</option>@foreach(['beginner','intermediate','advanced'] as $level)<option @selected(request('level')===$level)>{{ $level }}</option>@endforeach</select><button class="btn">Find courses</button></form><div class="grid cols-3">@forelse($courses as $course)@include('components.course-card')@empty<div class="card empty"><strong>No courses found.</strong>Try a different search or category.</div>@endforelse</div>{{ $courses->links() }}</div></section>@endsection