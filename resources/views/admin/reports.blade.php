@extends('layouts.app') @section('title','Reports & analytics') @section('content')<div class="page-head"><div><h1>Reports & analytics</h1><p>Understand your academy’s progress and impact.</p></div></div><div class="grid cols-3" style="margin-bottom:24px">@foreach(['Active courses'=>$courses->where('status','published')->count(),'Total enrollments'=>$courses->sum('enrollments_count'),'Completed journeys'=>$courses->sum('completed_count')] as $label=>$value)<div class="card pad"><span class="stat-label">{{ $label }}</span><div class="stat-value">{{ $value }}</div></div>@endforeach</div><div class="card" style="margin-bottom:24px"><div class="inset"><h3>Course & instructor performance</h3></div><div class="table-wrap"><table><thead><tr><th>Course</th><th>Instructor</th><th>Learners</th><th>Completed</th><th>Completion rate</th></tr></thead><tbody>@forelse($courses as $course)@php($rate=$course->enrollments_count?round($course->completed_count/$course->enrollments_count*100):0)<tr><td>{{ $course->title }}</td><td>{{ $course->instructor->name }}</td><td>{{ $course->enrollments_count }}</td><td>{{ $course->completed_count }}</td><td><div class="row"><div class="progress" style="width:100px"><span style="width:{{ $rate }}%"></span></div>{{ $rate }}%</div></td></tr>@empty<tr><td colspan="5" class="empty">Course performance will appear here.</td></tr>@endforelse</tbody></table></div></div><div class="card"><div class="inset"><h3>Student, enrollment & certificate report</h3></div><div class="table-wrap"><table><thead><tr><th>Student</th><th>Course</th><th>Status</th><th>Progress</th><th>Certificate</th></tr></thead><tbody>@forelse($enrollments as $enrollment)<tr><td>{{ $enrollment->user->name }}<small style="display:block">{{ $enrollment->user->email }}</small></td><td>{{ $enrollment->course->title }}</td><td><span class="badge">{{ $enrollment->status }}</span></td><td>{{ $enrollment->progress }}%</td><td>{{ $enrollment->certificate?->number??'Not yet issued' }}</td></tr>@empty<tr><td colspan="5" class="empty">No enrollments yet.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $enrollments->links() }}</div></div>@endsection
